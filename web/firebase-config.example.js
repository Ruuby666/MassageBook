// Copy this file to firebase-config.js (gitignored) and fill in the real
// values from Firebase console > Project settings > General > Your apps >
// SDK setup and configuration. This file has no build step — it's loaded
// directly by the browser as a static ES module — so each checkout keeps
// its own local firebase-config.js pointing at its own Firebase project.
export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
